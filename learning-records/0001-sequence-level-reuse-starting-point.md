# Sequence-level reuse is the starting point

The learner correctly identified that annotation reuse is organized around the
protein sequence rather than a FASTA filename, project, or input identifier.
The learner currently proposes “same tool version” as the remaining reuse
condition; the next lesson should refine this into SeqEvi's complete evidence
identity before advancing to storage and concurrency.

## Evidence

In the initial diagnostic, the learner stated that when the invoked tool version
is the same, annotation should happen only once because the unit is the sequence.
